Documentation -> https://playwright.dev/docs/pom
Documentation -> https://playwright.dev/docs/test-typescript

```Bash
project/
│
├── pages/
│   └── LoginPage.ts
│
├── tests/
│   └── login.spec.ts 
```

```Typescript title="login-page.ts"
import {Page, Locator} from '@playwright/test';

export class LoginPage{
	//fields
	readonly page: Page;
	readonly username: Locator;
	readonly password: Locator;
	readonly submitButton: Locator;

	//Constructor
	constructor(page: Page){
		this.page = page;
		this.username = page.locator("xpath=//input[@id='user-name']");
		this.password = page.locator("xpath=//input[@id='password']");
		this.submitButton = page.locator("");
	}

	async goto(){
		await this.page.goto('https://www.saucedemo.com/');
	}

	async login(user: string, pass: string){
		await this.username.fill(user);
		await this.password.fill(pass);
		await this.submitButton.click();
	}
}

```

```Typescript title="test1.spec.ts"
import {test,expect} from '@playwright/test';
import {LoginPage} from '../pages/login-page';

test('Practicing Page Object Model Implementation',async({page})=>{
	const lp = new LoginPage(page);	
	await lp.goto();
	await lp.login('John','1234');

});

```


