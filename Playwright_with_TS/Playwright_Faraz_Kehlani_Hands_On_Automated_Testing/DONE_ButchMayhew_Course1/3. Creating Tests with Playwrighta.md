==Tip : Chrome >> Network Tab >> Fetch/XHR (filters to API requests and get rid of clutter).

==Use of Codegen to speed up testing.

Website for testing -> https://practicesoftwaretesting.com/

### Overview of Locator Strategies in Playwright.
Playwright interacts with DOM using 2 types of locators
1. Recommended Built-In locators
2. Legacy locators like XPath, CSS etc

## Overview of assertions in Playwright
With Playwright, there are two general categories of assertions
1. Locator assertions
2. Value assertions

Locator assertions are special in that this type of assertion fails, it will automatically get retried until it either passes or it reaches the timeout. This wait also acts as an implicit wait in your tests.

The value assertion, on the other hand, will be evaluated one time and either pass or fail.

Eg of Locator assertions
```Typescript
await expect(locator).toBeVisible()
await expect(page).toHaveURL()
await expect(locator).toHaveCount()
await expect(locator).toContainText()
```

Eg of Value assertions, notice this wait doesnt have`await`
```Typescript
expect(value).toBe()
expect(value).toContain()
expect(value).toBeTruthy()
expect(value).toEqual()
expect(value).ToHaveLength()
```

```Typescript title='example.spec.ts'
test('Home page",async({page})=>{
	//check the count of items displayed
	const productGrid = page.locator(".col-md-9");
	await expect(productGrid.getByRole("link")).toHaveCount(9); //locator assertion.
	expect(await productGrid.getByRole("link").count()).toBe(9); //Value Assertions.
	
	
})
```

## Structuring Playwright tests.
- Describe block
- before all, after all
- before each, after each

Before fix:
```Typescript title='home.spec.ts'
import { test, expect } from "@playwright/test";

test("Home page", async ({ page }) => {
  await page.goto("https://practicesoftwaretesting.com/");
  // Ensure the sign-in link is present
  await expect(page.getByTestId("nav-sign-in")).toHaveText("Sign in");

  // Check the title of the page
  await expect(page).toHaveTitle("Practice Software Testing - Toolshop - v5.0");

  // Check the count of items displayed
  const productGrid = page.locator(".col-md-9");
  await expect(productGrid.getByRole("link")).toHaveCount(9);
  expect(await productGrid.getByRole("link").count()).toBe(9);

  // Search for Thor Hammer and check the result
  await page.getByTestId("search-query").fill("Thor Hammer");
  await page.getByTestId("search-submit").click();
  await expect(productGrid.getByRole("link")).toHaveCount(1);
  await expect(page.getByAltText("Thor Hammer")).toBeVisible();
});
```

After fix:
```Typescript title="home.spec.ts"
import { test, expect } from "@playwright/test";

test.describe("Home page", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("https://practicesoftwaretesting.com/");
  });
  test("check sign in", async ({ page }) => {
    await expect(page.getByTestId("nav-sign-in")).toHaveText("Sign in");
  });

  test("validate page title", async ({ page }) => {
    await expect(page).toHaveTitle(
      "Practice Software Testing - Toolshop - v5.0"
    );
  });

  test("grid loads with 9 items", async ({ page }) => {
    const productGrid = page.locator(".col-md-9");
    await expect(productGrid.getByRole("link")).toHaveCount(9);
    expect(await productGrid.getByRole("link").count()).toBe(9);
  });

  test("search for Thor Hammer", async ({ page }) => {
    const productGrid = page.locator(".col-md-9");
    await page.getByTestId("search-query").fill("Thor Hammer");
    await page.getByTestId("search-submit").click();
    await expect(productGrid.getByRole("link")).toHaveCount(1);
    await expect(page.getByAltText("Thor Hammer")).toBeVisible();
  });
});
```

## Handling cookie authentication in Playwright.

```Typescript title='auth.setup.ts'
import {test as setup, expect} from "@playwright/test"; //Using this below syntax makes it so i can type setup rather than test when creating my setup file.

setup("Create customer 01 auth", async ({page,context})=>{
	const email = "customer@practicesoftwaretesting.com";
	const password = "welcome01";
	const customer01AuthFile = ".auth/customer01.json";
	
	await page.goto("https://practicesoftwaretesting.com/auth/login");
	await page.getByTestId("email").fill(email);
	await page.getByTestId("password").fill(password);
	await page.getByTestId("login-submit").click();
	
	await expect(page.getByTestId("nav-menu")).toContainText("Jane Doe");
	await context.storageState({path: customer01AuthFile});
	
});

```

```Typescript title="home.spec.ts"
import { test, expect } from "@playwright/test";

test.describe("Home page with no auth", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("https://practicesoftwaretesting.com/");
  });
  test("check sign in", async ({ page }) => {
    await expect(page.getByTestId("nav-sign-in")).toHaveText("Sign in");
  });

  test("validate page title", async ({ page }) => {
    await expect(page).toHaveTitle(
      "Practice Software Testing - Toolshop - v5.0"
    );
  });
});

test.describe("Home page customer 01 auth",()=>{
	test.use({storageState: ".auth/customer01.json" });
	test.beforeEach(async ({page}) => {
	    await page.goto("https://practicesoftwaretesting.com/");
	} )
	
  test("check customer 01 sign in", async ({ page }) => {
    await expect(page.getByTestId("nav-sign-in")).not.toBeVisible();
	await expect(page.getByTestId("nav-menu")).toContainText("Jane Doe");
  });
});
```

Similarly we could create an admin setup step, customer02 setup step.

## Visual Testing in Playwright

```Typescript title="home.spec.ts"
import { test, expect } from "@playwright/test";

test.describe("Home page with no auth", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("https://practicesoftwaretesting.com/");
  });
  
  test("visual test", async ({ page }) => {
    await expect(page).toHaveScreenshot("home-page-no-auth.png"));
  });
  
  
  test("check sign in", async ({ page }) => {
    await expect(page.getByTestId("nav-sign-in")).toHaveText("Sign in");
  });

  test("validate page title", async ({ page }) => {
    await expect(page).toHaveTitle(
      "Practice Software Testing - Toolshop - v5.0"
    );
  });
});

test.describe("Home page customer 01 auth",()=>{
	test.use({storageState: ".auth/customer01.json" });
	test.beforeEach(async ({page}) => {
	    await page.goto("https://practicesoftwaretesting.com/");
	} )
	
  test("visual test authorized", async ({ page }) => {
    await expect(page).toHaveScreenshot("home-page-customer01.png"));
  });
  
  test("check customer 01 sign in", async ({ page }) => {
    await expect(page.getByTestId("nav-sign-in")).not.toBeVisible();
	await expect(page.getByTestId("nav-menu")).toContainText("Jane Doe");
  });
});
```

//See what the below command does
```powershell
npx playwright test tests/home.spec.ts --update-snapshots
```


==TODO : if we want to mask part of our website which might be hidden due to ads etc.
```Typescript title="home.spec.ts"
import { test, expect } from "@playwright/test";

test.describe("Home page with no auth", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("https://practicesoftwaretesting.com/");
  });
  
  test("visual test", async ({ page }) => {
	// to have consistent browser state
	await page.waitForLoadState("networkidle");
	
	//masking
    await expect(page).toHaveScreenshot("home-page-no-auth.png",{
    mask:[page.getByTitle("Practice Software Testing - Toolshop")]
    }));
  });
});
```

Note- When running Visual tests in CI servers and they have different operating servers than the tests you're building the tests on or generating the snapshots on, you will have to generate CI specific snapshots. There are ways to do that using docker containers
https://playwright.dev/docs/test-snapshots

### API testing in Playwright
```Typescript title="api.spec.ts"
import {test, expect} from "@playwright/test"

test("GET /products",async({request})=>{
	const apiURL = https://api.practicesoftwaretesting.com";
	const response = await request.get(apiURL+"/products");
	
	expect(response.status()).toBe(200);
	const body = await response.json();
	console.log(body);
	expect(body.data.length).toBe(9);
	expect(body.total).toBe(50);
});

test("POST /users/login", async({request})=>){
	const apiURL = https://api.practicesoftwaretesting.com";
	const response = await request.post(apiURL+"/users/login",{
	data : {
		email: "customer@practicesoftwaretesting.com",
		password: "welcome01",
	},
	});
	
	expect(response.status()).toBe(200);
	const body = await response.json();
	console.log(body);
	expect(body.access_token).toBeTruthy();
}
```

==TODO : check what Chrome>>Network tab (preserve log checkbox does).

## Automating the right things with playwright
- Unit/component tests
- API regression tests (playwright helpful here)
- UI regression tests (playwright helpful here)
- Smoke tests (playwright helpful here)
- Performance tests
- Security tests

![[Pasted image 20260705015312.png]]

==TODO - Create a automated test a 
- e2e UI flow, 
- visual tests and 
- api tests.

